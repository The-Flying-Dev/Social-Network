<div class="all_posts">
  <h4>All Posts</h4>

  <% @posts.each do |post| %>
  
    <%= link_to post.user.username, main_app.profile_path(post.user.username) %> Posted
  <ul>
    <li><%= post.content %></li>
    <% (0...post.images.count).each do |image| %>    
        <li><%= image_tag(post.images[image], class: "image-style") if post.images.attached? %></li>
      <% end %>
    
    <%= link_to "#{time_ago_in_words post.created_at} ago", main_app.post_path(post) %>
  </ul>

  <% end %>
</div>